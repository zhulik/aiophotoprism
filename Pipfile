[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
aiohttp = ">=3.7.4, <4.0.0"
yarl = ">=1.6.3, <2.0.0"

[dev-packages]
aioresponses = ">=0.7.1"
coverage = ">=5.5"
expects = ">=0.9.0"
flake8 = ">=3.8.4"
flake8-docstrings = ">=1.5.0"
isort = ">=5.7.0"
pylint = ">=2.7.2"
pytest = ">=6.2.2"
pytest-asyncio = ">=0.14.0"
pytest-cov = ">=2.11.1"
pytest-mock = ">=3.5.1"
black = ">=20.8b1"
pip = ">=21.0.1"
pre-commit = ">=2.10.1"
wheel = ">=0.36.2"

[pipenv]
allow_prereleases = true
